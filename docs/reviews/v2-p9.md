# V2-P9 — Self Review (TokenJuice 압축, V2-P10)

| | |
|---|---|
| **Phase** | V2-P10 — LLM 전단 토큰 압축 미들웨어 |
| **Status** | ✅ 통과 (clippy 0 warning, 전체 139 test pass) |
| **Date** | 2026-05-28 |

## ① 변경 요약
- `luida-core/agents/tokenjuice.rs` — `compress_context(input, budget_chars)`
  - 예산 이내면 그대로 통과
  - 초과 시: `tidy`(3+ 연속 빈 줄→1, 인접 중복 줄 제거, 우측 트림) → 여전히 초과면 char-safe 절단(+말줄임 표시)
- `luida-brain/ingest`: 수집 컨텍스트를 `compress_context(blob, 6000)`으로 압축 후 LLM에 전달 (첫 소비처)

## ② 설계 의사결정
- **2단계(정리→절단)**: 중복·공백만으로 예산에 들어오면 정보 손실 없이 정리만. 그래도 크면 절단.
- **char(코드포인트) 경계 절단**: CJK 안전. 정밀 grapheme(이모지 ZWJ)은 `unicode-segmentation` 의존 필요 → 후속(spec §5.4 Intl.Segmenter 대응).
- **말줄임 표시 예산 포함**: 표시 길이만큼 본문을 줄여 총 char 수가 budget을 넘지 않음. budget이 표시보다 작으면 표시 생략.

## ③ 잔여
- HTML→Markdown 변환·URL 단축·도구결과 요약(spec §5.4 세부)은 후속. 현재는 공백/중복/절단.
- 적용처 확대(campaign.plan 다중 프로젝트 맥락, quest.review diff 등)는 해당 행위 구현 시.

## 검증 증거
- `cargo clippy --all-targets -- -D warnings` → PASS (0 warning)
- `cargo test` → **139 passed; 0 failed**
- tokenjuice: 소입력 무변경 / 빈줄·중복 정리 / 정리만으로 예산충족 / 초과 char-safe 절단 / tiny budget 표시생략 / budget 0 / 표시 포함
