# V2-P7 — Self Review (TUI 대시보드 패널)

| | |
|---|---|
| **Phase** | TUI 모니터링 — Projects/Campaigns/Quests 탭 + escalation 카운트 |
| **Status** | ✅ 통과 (clippy 0 warning, TUI 4 test, 전체 126 test pass) |
| **Date** | 2026-05-28 |

## ① 변경 요약
- `Dashboard { projects, campaigns, quests, pending_user_mail }` + `Dashboard::load(conn)` — 순수 DB 조회(단위테스트 가능)
- 탭 전환(`Tab` 키): 모험지/원정/모험. `j/k` 이동, `q` 종료
- 헤더 탭 바 + `@user` 미배달 inmail 수를 `🔔 판단대기 N`으로 표시
- quest 행은 progress 우선 표시, status별 색상(완료=초록/실패=빨강/needs_input=주황/running=골드)

## ② 설계 의사결정
- **데이터 로딩과 렌더 분리**: `Dashboard::load`·`tab_items`·`App` 네비게이션을 순수 함수/상태로 빼서 ratatui 렌더 없이 단위테스트. 렌더(`draw`)는 thin.
- **빈 상태 힌트**: 각 탭에 다음 행동(CLI 명령) 안내.

## ③ 잔여
- 상세 뷰(원정→quest 드릴다운)·등록 폼·실시간 갱신(폴링/SSE 연동)은 후속.
- server POST command API + Tauri GUI는 별도 UI Phase.

## 검증 증거
- `cargo clippy --all-targets -- -D warnings` → PASS (0 warning)
- `cargo test` → 126 passed; 0 failed
- TUI: Dashboard 카운트 / 탭전환·네비 경계 / tab_items 행수 / 빈 상태 무선택
